<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nctigba.observability.instance.mapper.CollectTemplateMetricsMapper">


    <insert id="insertBatchTemplateMetrics">
        insert into collect_template_metrics(template_id, metrics_key, interval, is_enable) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.templateId},#{item.metricsKey},#{item.interval},#{item.isEnable})
        </foreach>
    </insert>

</mapper>
