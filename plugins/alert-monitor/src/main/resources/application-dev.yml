context:
  listener:
    classes:
spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
  datasource:
    dynamic:
      primary: master
      datasource:
        master:
          url: jdbc:opengauss://127.0.0.1:7001/postgres?currentSchema=public
          driverClassName: org.opengauss.Driver
          username: scott
          password: '******'
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      test-while-idle: false
      test-on-borrow: true
      validation-query: "select 1"
      validation-query-timeout: 10000
      connection-error-retry-attempts: 0
      break-after-acquire-failure: true
      max-wait: 3000
  sql:
    init:
      mode: always
      data-locations: classpath:db/alert-monitor.sql
      continue-on-error: true

mybatis-plus:
  typeAliasesPackage: org.opengauss.plugin.alertcenter.entity,org.opengauss.admin.common.core.domain.entity